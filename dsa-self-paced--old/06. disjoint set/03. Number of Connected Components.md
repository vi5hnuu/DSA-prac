https://www.geeksforgeeks.org/batch/dsa-4/track/DSASP-DisjointSet/problem/number-of-connected-components

### Why is this wrong

```cpp
// User function Template for C++

class Solution {
  public:
    int find(int a, int par[], int n) {
        if(par[a]==a) return a;
        return (par[a]=find(par[a],par,n));
    }

    void unionNodes(int a, int b, int par[], int rank[], int n) {
       int px=find(a,par,n);
        int py=find(b,par,n);

        if(px==py) return;

       if (rank[px] < rank[py]) {
            par[px] = py;
        } else if (rank[px] > rank[py]) {
            par[py] = px;
        } else {
            par[py] = px;
            rank[px]++;
        }
    }

    // Function to determine number of connected components.
    int findNumberOfConnectedComponents(int n, int par[], int rank[]) {//edges n=5 => 0-5 can also be there
        vector<bool> visited(n,false);
        for(int i=0;i<=n;i++){
            visited[par[i]]=true;
        }

        int cs=0;
        for(auto v:visited) cs+=v;
        return cs;
    }
};
```

```cpp
// User function Template for C++

class Solution {
  public:
    int find(int a, int par[], int n) {
        if(par[a]==a) return a;
        return (par[a]=find(par[a],par,n));
    }

    void unionNodes(int a, int b, int par[], int rank[], int n) {
       int px=find(a,par,n);
        int py=find(b,par,n);

        if(px==py) return;

        if (rank[px] < rank[py]) {
            par[px] = py;
        } else if (rank[px] > rank[py]) {
            par[py] = px;
        } else {
            par[py] = px;
            rank[px]++;
        }
    }

    // Function to determine number of connected components.
    int findNumberOfConnectedComponents(int n, int par[], int rank[]) {//edges n=5 => 0-5 can also be there
        unordered_set<int> uniqueParents;
        for (int i = 1; i <= n; i++) {
            uniqueParents.insert(find(i, arr,n));
        }
        return uniqueParents.size();
    }
};
```
