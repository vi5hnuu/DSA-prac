https://www.geeksforgeeks.org/batch/dsa-4/track/DSASP-Graph/problem/level-of-nodes-1587115620

```cpp

class Solution
{
	public:
	int nodeLevel(int V, vector<int> adj[], int X)
	{
	    //bfs //level order traversal in tree + visited array to not visit again
	    bool visited[V]={false};
	    queue<int> nodes;
	    nodes.push(0);
	    visited[0]=true;
	    int level=0;
	    while(!nodes.empty()){
	        int sz=nodes.size();
	        while(sz--){
	            int node=nodes.front();
	            nodes.pop();
	            for(const int &x:adj[node]){
	                if(visited[x]) continue;
                    if(x==X) return level+1;
                    visited[x]=true;
                    nodes.push(x);
	            }
	        }
	        level++;
	    }
	    return -1;

	}
};
```
