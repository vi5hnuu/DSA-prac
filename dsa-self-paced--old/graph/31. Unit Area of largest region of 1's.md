https://www.geeksforgeeks.org/batch/dsa-4/track/DSASP-Graph/problem/length-of-largest-region-of-1s-1587115620

```cpp
class Solution{
private:
    int dfsArea(int i,int j,vector<vector<int>> &grid,const int &r,const int &c){
        if(!grid[i][j]) return 0;
        grid[i][j]=0;//mark visited [area counted]

        int count=1;
        int dx[]={0,0,1,-1,1,-1,1,-1};
        int dy[]={1,-1,0,0,1,-1,-1,1};
        for(int k=0;k<8;k++){
            int xi=i+dx[k];
            int yj=j+dy[k];
            if(xi>=0 && xi<r && yj>=0 && yj<c && grid[xi][yj]){
                count+=dfsArea(xi,yj,grid,r,c);
            }
        }
        return count;
    }
public:
    int findMaxArea(vector<vector<int>>& grid) {
        int rows=grid.size();
        int cols=grid[0].size();

        int mxArea=0;
        for(int i=0;i<rows;i++){
            for(int j=0;j<cols;j++){
                if(grid[i][j]){
                  int area=dfsArea(i,j,grid,rows,cols);
                  mxArea=max(mxArea,area);
                }
            }
        }
        return mxArea;
    }
};
```
